# must be ended with a new line "LF" (Unix) and not "CRLF" (Windows)
0 */2 * * * eval "/usr/local/bin/node /cf2dns/lib/cli.js optimize $(echo $DOMAINS | sed 's/,/ /g') --secretId=$ID --secretKey=$SECRET --key=$KEY >> /var/log/cron.log 2>&1"
# An empty line is required at the end of this file for a valid cron file.
